{% macro service(shadow, color, heading, paragraph) %}
<div class="bg-slate-200 p-6 rounded-md border border-gray-100 {{ shadow }}">
  <div class="w-12 h-1.5 {{ color }}"></div>
  <h3 class="text-3xl mt-2">{{ heading }}</h3>
  <p class="mt-2">{{ paragraph }}</p>
</div>
{% endmacro %}
